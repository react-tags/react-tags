export enum KEYS {
  ENTER = 13,
  TAB = 9,
  BACKSPACE = 8,
  UP_ARROW = 38,
  DOWN_ARROW = 40,
  ESCAPE = 27,
  SPACE = 32,
}

export const DEFAULT_PLACEHOLDER = 'Press enter to add a new tag';

export const DEFAULT_LABEL_FIELD = 'text';
